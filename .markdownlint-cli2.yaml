# Ignore long-form planning/investigation docs that contain many code snippets,
# wide tables and long reference lines (not product-facing markdown).
ignores:
  - "PLAN.md"
  - "RECOMMENDATIONS.md"

rules:
  MD013:
    line_length: 200
    code_blocks: false
    tables: false
